Internal dev notes for BLACKBOXAI.

- Current known changes: 
  - /register public
  - POS dashboard stats collapsed by default
  - logout + inventory modal backdrop blur softened to ~1px

- Remaining work requested by user:
  - Switch login/register from store name to email
  - Register collects email + store name
  - Settings page (profile in /settings) shows email read-only (not editable)
  - Sidebar shows store name (not email)
  - Mobile POS uses same Navigation via protected layout

